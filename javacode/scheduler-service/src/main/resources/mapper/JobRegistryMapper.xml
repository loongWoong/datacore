<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dataplatform.scheduler.mapper.JobRegistryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.dataplatform.scheduler.entity.JobRegistry">
        <id column="id" property="id"/>
        <result column="registry_group" property="registryGroup"/>
        <result column="registry_key" property="registryKey"/>
        <result column="registry_value" property="registryValue"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, registry_group, registry_key, registry_value, update_time
    </sql>

    <!-- 根据注册组和注册键查询注册信息 -->
    <select id="selectByRegistryGroupAndKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM xxl_job_registry
        WHERE registry_group = #{registryGroup} AND registry_key = #{registryKey}
    </select>

    <!-- 删除指定时间之前的注册信息 -->
    <delete id="deleteByUpdateTimeBefore">
        DELETE FROM xxl_job_registry WHERE update_time &lt; #{beforeTime}
    </delete>

</mapper>